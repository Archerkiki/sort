#include <stdio.h>
#include "include/sort.h"

int main() {
        int a[10]={23,4,6,28,36,23,1};
        int n=7;
        maopao(a,n);
        D_insert(a,n);
        choose_sort(a,n);
        return 0;
}
